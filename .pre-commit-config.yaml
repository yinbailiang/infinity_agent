repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-ast
      - id: check-toml
      - id: check-yaml
      - id: check-json
      - id: check-merge-conflict
      - id: check-case-conflict
      - id: end-of-file-fixer
        exclude: '\.py$'
      - id: trailing-whitespace
        exclude: '\.py$'

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.7
    hooks:
      - id: ruff-check
        args: ["--fix"]
        files: ^src/
      - id: ruff-format
        files: ^src/

  - repo: https://github.com/RobertCraigie/pyright-python
    rev: v1.1.403
    hooks:
      - id: pyright
        args: ["--level", "error"]
        files: ^src/

  - repo: https://github.com/econchick/interrogate
    rev: 1.7.0
    hooks:
      - id: interrogate
        files: ^src/

  - repo: local
    hooks:
      - id: pytest-cov
        name: pytest coverage
        entry: uv run pytest tests/ --cov=infinity_agent --cov-fail-under=80 -q
        language: system
        pass_filenames: false
        always_run: true
        types: [python]

      - id: interrogate-badge
        name: interrogate badge
        entry: uv run interrogate src/infinity_agent/ --generate-badge docs/res/interrogate_badge.svg
        language: system
        pass_filenames: false
        always_run: true
